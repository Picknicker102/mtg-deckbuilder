# Oracle data

Place a local Scryfall Oracle dump here (e.g. `oracle.json`). The rule engine will consume it when `ORACLE_JSON_PATH` is set (default: `backend/data/oracle.json`).

Do **not** commit the real oracle file to version control; keep it ignored in `.gitignore`.
